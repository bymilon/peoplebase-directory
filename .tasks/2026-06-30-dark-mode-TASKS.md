# Dark Mode Implementation Tasks

## Phase 1: Setup & Infrastructure
- [x] Initialize this task tracking file.
- [x] Add `@variant dark` configuration to `index.css`.
- [x] Create `ThemeProvider.tsx` to handle `next-themes`-style dark mode state.
- [x] Wrap the application in `ThemeProvider` within `App.tsx` or `main.tsx`.

## Phase 2: Navigation & Layout
- [x] Connect the dark mode toggle switch in `Sidebar.tsx`.
- [x] Apply dark mode classes to `App.tsx` layout backgrounds and text colors.
- [x] Apply dark mode classes to `Sidebar.tsx` backgrounds, text, and borders.
- [x] Apply dark mode classes to `Header.tsx` backgrounds, text, and borders.

## Phase 3: Dashboard Components
- [x] Apply dark mode classes to `KpiCards.tsx`.
- [x] Apply dark mode classes to `CommunicationFunnel.tsx`.
- [x] Apply dark mode classes to `OutreachConversion.tsx`.
- [x] Apply dark mode classes to `SequenceStepActivity.tsx`.
- [x] Apply dark mode classes to `MessageSentChart.tsx`.

## Phase 4: Final QA
- [x] Verify dark mode looks cohesive across the entire application.
- [x] Check contrast ratios for text and borders.
- [x] Mark all tasks as complete.
