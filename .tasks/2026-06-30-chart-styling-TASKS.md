# Chart Styling Tasks

## Phase 1: Recharts Gel Aesthetic
- [x] Read `MessageSentChart.tsx` and `OutreachConversion.tsx`.
- [x] Define SVG gradients for all chart colors (Cyan, Green, Blue, Purple) in the `<defs>` of both charts.
- [x] Implement SVG filters for inner highlights, inner shadows, and outer shadows (the 3D gel effect) within the `<defs>` of both charts.
- [x] Apply the gradients and filters to the `Bar` components in `MessageSentChart`.
- [x] Update the diagonal hatch pattern in `MessageSentChart` to use the new gradient background.
- [x] Apply the gradients and filters to the `Cell` components inside the `PieChart` in `OutreachConversion`.

## Phase 2: Review and Consistency
- [x] Ensure the visual aesthetic closely matches the gel-style buttons and communication funnel.
- [x] Mark tasks as complete.
